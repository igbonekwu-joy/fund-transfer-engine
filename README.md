# Fund Transfer Engine

A mini fintech application that moves money between user wallets using a double-entry ledger.

The ledger and peer-transfer core live at the **service layer** at the moment. Auth, profile, and KYC are exposed over HTTP; transfer/deposit HTTP APIs are not wired yet.

---

## Stack

| Layer | Technology |
|-------|------------|
| Backend | PHP 8.3, Laravel 13 |
| API client | React 19, TypeScript, Vite, TanStack Query, Axios (`client/`) |
| Auth (API) | Laravel Sanctum personal access tokens in httpOnly cookies + custom CSRF |
| Ledger | Double-entry `transactions` + immutable `ledger_entries` |
| Cache / rate limits | Redis (`phpredis`) |
| Docs | L5-Swagger (`/api/documentation`) |
| Tests | Pest 4 |

---

## Requirements

- PHP **8.3+** with extensions: `pdo_mysql`, `redis`, `mbstring`, `openssl`, `tokenizer`, `xml`, `ctype`, `json`, `bcmath`
- Composer 2
- Node.js **20+** (CI uses 22)
- MySQL **8.x** (or MariaDB) for local development
- Redis **7.x** (login sliding-window rate limit and default cache)

Laragon (Windows) or equivalent local stack works well.

---

## Quick start

From the repository root:

```bash
# 1. Install PHP dependencies and scaffold env
composer install
cp .env.example .env
php artisan key:generate

# 2. Configure .env (see Environment below), then:
#    CREATE DATABASE fund_transfer_engine;

php artisan migrate --seed
# SystemAccountsSeeder creates the money_in ledger boundary account

# Or one-shot helper (installs, migrates, builds):
# composer setup
```

React SPA:

```bash
cd client
cp .env.example .env
# Set VITE_BACKEND_URL=http://localhost:8000/api/v1
npm install
npm run dev
```

---

## Environment

Copy `.env.example` to `.env` and set at least:

| Variable | Purpose |
|----------|---------|
| `APP_KEY` | Generated by `php artisan key:generate` |
| `APP_URL` | Backend URL (e.g. `http://localhost:8000`) |
| `DB_*` | MySQL connection (`fund_transfer_engine` by default) |
| `REDIS_*` | Redis for cache and login rate limiting |
| `CACHE_STORE` | Prefer `redis` locally to match production-like rate limits |
| `CORS_ALLOWED_ORIGINS` | Comma-separated origins allowed for credentialed API calls (e.g. `http://localhost:5173`) |
| `SESSION_SECURE_COOKIE` | `false` on local HTTP; `true` behind HTTPS |
| `L5_SWAGGER_ENABLED` | `true` to expose Swagger UI |
| `KYC_USE_FAKE_PROVIDERS` | Keep `true` for local/dev KYC heuristics |

**Client** (`client/.env`):

| Variable | Purpose |
|----------|---------|
| `VITE_BACKEND_URL` | API base, typically `http://localhost:8000/api/v1` |
| `VITE_APP_NAME` | Display name |
| `VITE_APP_ENV` | Optional environment label |

Login and register require a matching `Origin` header in `CORS_ALLOWED_ORIGINS`.

---

## Running the apps

### Backend + Livewire Vite + queue

```bash
composer dev
```

Runs `php artisan serve`, queue listener, and root Vite together.

Or separately:

```bash
php artisan serve
php artisan queue:work
npm run dev
```

### React client

```bash
cd client
npm run dev
```

Point the browser at the Vite URL (usually `http://localhost:5173`) with `VITE_BACKEND_URL` aimed at the Laravel API.

---

## Architecture

### High-level layout

```text
┌─────────────────────┐         cookie + CSRF          ┌──────────────────────┐
│  React SPA (client) │ ─────────────────────────────► │  Laravel API (/api/v1)│
└─────────────────────┘                                │  Auth / Profile / KYC │
                                                       └──────────┬───────────┘
                                                        │  Services + Ledger
                                                       │  TransferService*
                                                       └──────────────────────┘
* Service layer only — no transfer HTTP routes yet
```

Two frontends share one backend:

1. **API + React SPA** — primary product surface (cookie Sanctum auth).
2. **Livewire settings** — server-rendered account settings and 2FA.

### Domain model

```text
User 1──* Account (user wallets)
User 1──1 KycVerification (optional)

Transaction 1──* LedgerEntry
Account      1──* LedgerEntry
```

- **User** — UUID primary key; profile fields; primary wallet via `account()`; Sanctum tokens; optional 2FA.
- **Account** — UUID; `type` (`user` | `system_inbound`); `status` (`active` | `frozen` | `closed`); currency (default `NGN`); optional `account_number` / `slug`.
- **Transaction** — Journal header: `type`, `status`, `metadata`; `isBalanced()` checks legs.
- **LedgerEntry** — Immutable debit/credit leg in **minor units (kobo)**; updates/deletes are blocked.
- **KycVerification** — Tier/status; sensitive IDs encrypted at rest.

Completing a profile (`ProfileService`) creates the user’s primary NGN wallet and assigns a random 10-digit `account_number`.

### Double-entry ledger

Every money movement is a **transaction** with at least two **ledger entries**. Debits must equal credits or `BalancedLedgerWriter` rejects the posting.

#### Conventions

| Account kind | Role | Balance (`balanceInMinorUnits`) |
|--------------|------|----------------------------------|
| User wallet | Liability | credits − debits |
| `money_in` (`SystemInbound`) | External money boundary (asset-like) | debits − credits |

#### System account: `money_in`

Seeded by `Database\Seeders\SystemAccountsSeeder` (`slug = money_in`).

Deposits (test helper and future funding flows) look like:

```text
Deposit:
  debit  money_in     (funds enter the system)
  credit user_wallet  (user balance increases)
```

Peer transfers never touch `money_in`:

```text
Transfer:
  debit  sender_wallet
  credit recipient_wallet
```

### Locking, deadlocks, and retries

**Why lock:** Without `SELECT … FOR UPDATE`, two concurrent transfers can both read the same balance and double-spend.

**Lock order:** `AccountLocker` always locks by ascending account UUID/string id. Opposing transfers (A→B and B→A) take locks in the same order, which prevents the classic deadlock cycle.

**Retry:** `TransferService` uses `DB::transaction(..., 2)` so a rare deadlock/serialization failure is retried once.

**Concurrency tests:** Real races are proven with child PHP processes against MySQL (see [Testing](#testing)). SQLite in-memory cannot honor cross-connection row locks the same way.

## HTTP API

Base path: `/api/v1`

| Method | Path | Auth | Notes |
|--------|------|------|-------|
| `POST` | `/auth/register` | Public (+ Origin) | Creates user |
| `POST` | `/auth/login` | Public (+ Origin, rate limited) | Sets auth cookies |
| `POST` | `/auth/refresh` | Refresh cookie | Rotates tokens |
| `POST` | `/auth/logout` | Sanctum | Clears tokens/cookies |
| `GET` | `/auth/user` | Sanctum | Current user |
| `POST` | `/user/profile` | Sanctum | Upsert profile + wallet |
| `GET` | `/user/kyc` | Sanctum | Current KYC state |
| `POST` | `/user/kyc` | Sanctum | Submit KYC |

**Not exposed yet:** deposit, peer transfer, withdrawal, balances, or transaction history endpoints. Call `TransferService` from tests or future controllers only.

Health check: `GET /up`.

---

## OpenAPI / Swagger

- Package: `darkaonline/l5-swagger`
- UI: `/api/documentation` (when `L5_SWAGGER_ENABLED=true`)
- Spec sources: `app/OpenApi/` and controller annotations
- Access gated by `RestrictSwaggerDocumentation` middleware

Regenerate docs as configured in `config/l5-swagger.php` (optional `L5_SWAGGER_GENERATE_ALWAYS`).

---

## Testing

### Default suite (SQLite)

`phpunit.xml` uses SQLite `:memory:` for Feature tests and includes the Concurrency suite (those tests **skip** on SQLite).

```bash
php artisan test
# or full gate:
composer test   # config:clear + Pint --test + PHPStan + Pest
```

### Concurrent transfer suite (MySQL)

Requires real row-level locks:

```sql
CREATE DATABASE fund_transfer_engine_testing
  CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

```bash
php artisan test -c phpunit.concurrency.xml
```

This runs `tests/Concurrency/TransferServiceConcurrencyTest.php`, which spawns `tests/bin/transfer_worker.php` processes to prove:

- no double-spend when two transfers exhaust the same sender
- both succeed when funds cover both
- opposing A↔B transfers complete without deadlock

`Tests\ConcurrencyTestCase` disables wrapping DB transactions so committed rows are visible across processes.

### Important ledger tests

| Area | Location |
|------|----------|
| Balanced postings / immutability / nested writer rollback | `tests/Feature/LedgerFoundationTest.php` |
| Transfer happy path, insufficient funds, self-transfer, mid-flight rollback, edge cases | `tests/Feature/TransferServiceTest.php` |
| Pure transfer rules | `tests/Feature/TransferGuardTest.php` |
| Ordered `FOR UPDATE` locks | `tests/Feature/AccountLockerTest.php` |
| Concurrent races | `tests/Concurrency/TransferServiceConcurrencyTest.php` |

Test funding helper: `fundAccount()` in `tests/Pest.php` (deposit via `money_in`).

---

## Code quality and CI

| Command | What it does |
|---------|----------------|
| `composer lint` / `composer lint:check` | Laravel Pint |
| `composer types:check` | PHPStan / Larastan |
| `composer test` | Lint + types + Pest |
| `composer ci:check` | Same gate used conceptually for CI |

GitHub Actions (`.github/workflows/tests.yml`) runs on `main` and pull requests with MySQL 8.4 and Redis 7 services, then `composer setup` / `composer ci:check`.

---

## License

MIT (see `composer.json`).
